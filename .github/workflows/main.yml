# .github/workflows/test.yml
# Этот файл будет проверять Node.js-код при каждом PR в dev

name: 🟢 Node.js проверка

# Когда запускать? → только при Pull Request в ветку dev
on:
  pull_request:
    branches: [ dev ]

jobs:
  test:
    runs-on: ubuntu-latest   # виртуальная машина Linux

    steps:
      # 1. Скачиваем твой код
      - uses: actions/checkout@v4

      # 2. Ставим Node.js (как у тебя на компе)
      - name: Устанавливаем Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Заходим в папку фронтенда и ставим пакеты
      - name: Устанавливаем зависимости
        run: |
          cd frontend
          npm install

      # 4. Запускаем тесты (пока просто "hello")
      - name: Тестируем
        run: |
          cd frontend
          echo "✅ Всё ок!" > ../result.txt

      # 5. Сохраняем результат, чтобы ты увидел
      - name: Загружаем лог
        uses: actions/upload-artifact@v4
        with:
          name: лог-теста
          path: result.txt
